import React from "react";

export default function App() {
  return (
    <div style={{ fontFamily: "system-ui", background: "#06080c", color: "#e4eaf4", minHeight: "100vh", padding: 24 }}>
      <h1>ClawdBot Mission Control</h1>
      <p>Frontend scaffold is running. Connect to API at <code>/api/health</code>.</p>
    </div>
  );
}
